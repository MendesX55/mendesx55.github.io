# GitHub Pages PWA setup

1. Rename `index.html` to the site root.
2. Put `sw.js` and `manifest.webmanifest` beside it.
3. Enable GitHub Pages from the repository settings.
4. Open the site in Chrome/Edge and use "Install app".

Note: the service worker only works on HTTPS or localhost, which GitHub Pages provides.
